# ApplyMate -- runtime dependencies.
# Only packages the app actually imports are listed; the Python standard library
# and transitive deps (e.g. numpy / scipy via scikit-learn) are omitted.

# UI framework. The app relies on st.navigation / st.Page, the width="stretch"
# widget sizing, st.container(border=...), and st-key-* element classes, all of
# which require a recent Streamlit.
streamlit==1.49.0

# Résumé upload/parsing (.pdf / .docx / .txt) + Word (.docx) export
pypdf>=4.3.0
python-docx>=1.1.2

# ATS keyword match -- TF-IDF + cosine similarity (pulls in numpy / scipy).
# scikit-learn replaces the originally-planned spaCy, which needs a ~12MB model
# downloaded at runtime; TF-IDF + a rule-based extractor is more robust here.
scikit-learn>=1.5.1

# AI tailoring on the ATS Match page -- OpenAI-compatible client used against
# OpenRouter (Tencent hy3). Base URL / model / key are all configurable.
openai>=1.40.0

# PDF export
reportlab>=4.2.2

# Job search -- Adzuna + Remotive HTTP APIs
requests>=2.31.0
