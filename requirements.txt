# ApplyMate -- runtime dependencies.
# Only packages the app actually imports are listed; the Python standard library
# is omitted. The app stays deliberately light -- no torch, no local ML model --
# so it deploys cleanly on the Streamlit Cloud free tier.

# UI framework. The app relies on st.navigation / st.Page, the width="stretch"
# widget sizing, st.container(border=...), and st-key-* element classes, all of
# which require a recent Streamlit.
streamlit==1.49.0

# Résumé upload/parsing (.pdf / .docx / .txt) + Word (.docx) export
pypdf>=4.3.0
python-docx>=1.1.2

# AI + semantic matching, both via the same OpenAI-compatible client:
#   * Chat (résumé tailoring) runs against OpenRouter -- base URL / model / key
#     are configurable (OPENROUTER_*).
#   * Embeddings (ATS match breakdown + RAG retrieval) run against any
#     OpenAI-compatible embeddings endpoint (EMBEDDINGS_*); the default is
#     Google Gemini's free tier (text-embedding-004), so semantic matching
#     costs nothing. Cosine/vector math is pure Python, so no
#     numpy/scipy/scikit-learn is needed, and the lexical fallback is stdlib-only.
openai>=1.40.0

# PDF export
reportlab>=4.2.2

# Job search -- Adzuna + Remotive HTTP APIs
requests>=2.31.0
